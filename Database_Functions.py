#import openpyxl
import os

def check_excelsheet():
    os.chdir('./Data/')
    if os.path.isfile('profiledata.xlsx'):
        print ("File Exists");
    else
    print ("File Doesn't exist");
    #Check for 'profiledata.xlsx'
    #Create an excel sheet if it document doesn't exist
'''
def grab_data(CC):
    wb = openpyx1.load_workbook('profiledata.xlsx')
    i = 1;
    #if command code is 0 Find e-mail with null password field and output e-mail
    if (CC == 0):
        while true:
            cell.get_row_letter
            
    #if command code is 1 find email- with null IU field and output password
    if (CC == 1):
        printf

def input_data(str1, str2, int):
    wb = openpyx1.load_workbook('profiledata.xlsx')
    #if str2 is null than input str1 into farthest part of database
    #if str2 is not null than look at int for which to execute
    #if command code is 0 input str2 into password field
    #if command code is 1 input str2 into IU field
'''
